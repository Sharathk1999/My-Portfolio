import 'package:flutter/material.dart';

class SpaceHeightBetween extends StatelessWidget {

  const SpaceHeightBetween({super.key,});

  @override
  Widget build(BuildContext context) {
    return  const SizedBox(
     
      height: 25,
    );
  }
}